// import { BlogCard } from "../components"
// import { useApiData } from "../hooks"
// import ComponentFallback from "../ErrorHandler/component-fallback";

export default function BlogBody(props) {
    // const { data, loading, error } = useApiData('http://localhost:7001/articles');

    
    //   if (error) {
    //     return <ComponentFallback />
    //   }
    

    return (
        <div className="bg-darkblue">
            
            {/* {.map((blog) => {
                return (
                    <BlogCard blog={blog} />
                )
            })} */}
        </div>
    )
}
